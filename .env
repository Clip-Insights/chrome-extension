# Production backend. For local backend dev, override in .env.local
VITE_API_URL=https://clipinsights-241407463290.europe-west1.run.app

# Used to obfuscate JWTs at rest (PBKDF2 -> AES-GCM). NOTE: client-side only,
# this is obfuscation, not real secrecy (see ARCHITECTURE.md A.12).
VITE_TOKEN_SECRET=my-strong-encryption-key
